apiVersion: v1
kind: Service
metadata:
  name: mlops-inference-service
spec:
  type: NodePort
  selector:
    app: mlops-inference
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
